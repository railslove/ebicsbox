#!/usr/bin/env ruby

# Load environment variables
require 'dotenv'
Dotenv.load

# Load environment
require 'bundler'
Bundler.setup(:default, :development)

# Load app
require './lib/epics/box'

# Start pry session
require 'pry'
pry
