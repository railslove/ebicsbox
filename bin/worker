#!/usr/bin/env ruby -w

# Load environment variables
require 'dotenv'
Dotenv.load

# Load environment
require 'bundler'
Bundler.setup(:default, :development)

# Load app
require './box/init'

# Start worker
Box::Worker.new.process!
